// KÈO ĐÊ — cấu hình Firebase (public-safe, không phải mật khẩu)
// Dán giá trị thật vào các dòng "..." bên dưới (Firebase Console → ⚙️ Project settings → Your apps → Web app)
// Sau đó commit + push là đăng nhập Google/Facebook chạy cho MỌI NGƯỜI trên keode.netlify.app
window.KEO_FIREBASE_CONFIG = null; // ← chưa cấu hình; thay null bằng object firebaseConfig
