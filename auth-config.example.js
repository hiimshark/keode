// KÈO ĐÊ — file cấu hình Firebase (public-safe, không phải mật khẩu)
// Cách dùng: đổi tên file này thành auth-config.js, điền giá trị từ
// Firebase Console → Project settings → Your apps → Web app → firebaseConfig
// rồi đặt cạnh keo-de.html. Khi đó người dùng không thấy màn hình cấu hình.
window.KEO_FIREBASE_CONFIG = {
  apiKey: "PASTE_API_KEY",
  authDomain: "your-app.firebaseapp.com",
  projectId: "your-app",
  appId: "1:123456789:web:abcdef"
};
